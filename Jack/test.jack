class Test {
    function void main() {
        var int i;
        let i = 0; // Initialisation
        return;
    }

    function void incrementCounter() {
        var int counter;
        let counter = 0; // Initialisation du compteur


         let counter = counter + 1; // Incrémentation
         while (counter < 5){
            let e = e +1;// Condition de sortie
         }

        return;
    }
}

