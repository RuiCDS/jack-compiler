class Main {
    function void main() {
        var Array myArray;
        var int result;
        var int currentPlayer;

        // Initialisation du tableau
        let myArray = Array.new(10);

        // Affectation d'une valeur au tableau
        let myArray[5] = 42;

        // Lecture de la valeur du tableau pour l'utiliser dans une expression


        do Output.printInt(result);

        return;
    }
}
